[package]
name = "openflow-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "C ABI over openflow-core, for the Swift clients."

[lib]
name = "openflow_ffi"
crate-type = ["staticlib"]
path = "src/lib.rs"

[dependencies]
openflow-core = { path = "../openflow-core" }
